[project]
name = "act-sesion-4"
version = "0.1.0"
description = "Aplicación de calculadora simple"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
testpaths = [
    "tests"
]
pythonpath = "."